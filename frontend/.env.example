# Empty = same-origin /api (Vite proxies it to localhost:8000).
# No provider API key belongs in the frontend.
VITE_API_BASE_URL=
# Set local for a deterministic demo without backend or external AI calls.
VITE_AI_MODE=auto
